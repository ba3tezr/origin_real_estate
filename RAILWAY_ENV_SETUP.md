# 🔧 إعداد المتغيرات البيئية في Railway

## 📝 الخطوات:

### 1️⃣ افتح لوحة تحكم Railway:
- اذهب إلى: https://railway.app/
- افتح مشروعك: **origin_real_estate**

### 2️⃣ اذهب إلى تبويب Variables:
- في صفحة المشروع، اضغط على تبويب **"Variables"**
- أو اضغط على **"Settings"** ثم **"Variables"**

### 3️⃣ أضف المتغيرات التالية واحدة تلو الأخرى:

---

## 🔑 المتغيرات البيئية المطلوبة:

### المتغير الأول: SECRET_KEY
```
Variable Name: SECRET_KEY
Value: 4m9f4d35!zqr-vc#h0$xxwt1ur19o^e##z0fsza7bqg4!ii^5)
```

### المتغير الثاني: DEBUG
```
Variable Name: DEBUG
Value: False
```

### المتغير الثالث: ALLOWED_HOSTS
```
Variable Name: ALLOWED_HOSTS
Value: originrealestate-production.up.railway.app,*.railway.app,localhost,127.0.0.1
```

### المتغير الرابع: CSRF_TRUSTED_ORIGINS
```
Variable Name: CSRF_TRUSTED_ORIGINS
Value: https://originrealestate-production.up.railway.app,https://*.railway.app
```

### المتغير الخامس: SECURE_SSL_REDIRECT
```
Variable Name: SECURE_SSL_REDIRECT
Value: False
```

---

## 📋 نسخ سريع (للنسخ واللصق):

يمكنك نسخ هذه القيم مباشرة:

**SECRET_KEY:**
```
4m9f4d35!zqr-vc#h0$xxwt1ur19o^e##z0fsza7bqg4!ii^5)
```

**DEBUG:**
```
False
```

**ALLOWED_HOSTS:**
```
originrealestate-production.up.railway.app,*.railway.app,localhost,127.0.0.1
```

**CSRF_TRUSTED_ORIGINS:**
```
https://originrealestate-production.up.railway.app,https://*.railway.app
```

**SECURE_SSL_REDIRECT:**
```
False
```

---

## 🎯 طريقة بديلة - استيراد من ملف:

إذا كان Railway يدعم استيراد المتغيرات من ملف:

1. استخدم ملف `railway.env` الموجود في المشروع
2. انسخ محتوياته والصقها في Railway

---

## ✅ التحقق من الإعدادات:

بعد إضافة المتغيرات:

1. **احفظ التغييرات**
2. **أعد نشر التطبيق** (Redeploy)
3. **تحقق من السجلات** (Logs) للتأكد من عدم وجود أخطاء

---

## 🗄️ ملاحظة مهمة عن قاعدة البيانات:

✅ **قاعدة البيانات SQLite محفوظة في المستودع:**
- ملف `db.sqlite3` موجود في GitHub
- سيتم نسخه تلقائيًا عند النشر
- يحتوي على جميع البيانات (المستخدمين، العقارات، إلخ)

⚠️ **تحذير:**
- أي تغييرات في البيانات على Railway ستُفقد عند إعادة النشر
- لحفظ البيانات بشكل دائم، يجب تحميل ملف `db.sqlite3` من Railway وإضافته للمستودع

---

## 🔄 لحفظ البيانات بعد التعديل:

إذا قمت بإضافة بيانات جديدة على Railway وتريد حفظها:

1. **حمّل ملف قاعدة البيانات من Railway:**
   - استخدم Railway CLI أو SSH
   - نزّل ملف `db.sqlite3`

2. **استبدل الملف المحلي:**
   ```bash
   # ضع الملف المحمّل مكان db.sqlite3
   ```

3. **ارفعه للمستودع:**
   ```bash
   git add db.sqlite3
   git commit -m "Update database with new data"
   git push origin main
   ```

---

## 🚀 بعد الإعداد:

زر التطبيق على:
**https://originrealestate-production.up.railway.app/**

بيانات الدخول:
- **اسم المستخدم:** admin
- **كلمة المرور:** admin123

---

## 📞 في حالة وجود مشاكل:

1. تحقق من السجلات (Logs) في Railway
2. تأكد من كتابة المتغيرات بشكل صحيح (بدون مسافات زائدة)
3. تأكد من أن الرابط في `ALLOWED_HOSTS` يطابق رابط Railway الخاص بك

---

**جاهز للنشر! 🎉**

