# 🚀 دليل النشر على Railway

## ✅ تم إعداد المشروع للنشر على Railway

### 📋 الملفات التي تم إنشاؤها:

1. **Procfile** - أوامر تشغيل التطبيق
2. **runtime.txt** - إصدار Python (3.11.9)
3. **railway.json** - إعدادات Railway
4. **requirements.txt** - تم تحديثه بالمكتبات المطلوبة للإنتاج
5. **.env.example** - نموذج للمتغيرات البيئية

---

## 🔧 إعدادات Railway المطلوبة

### المتغيرات البيئية (Environment Variables):

قم بإضافة المتغيرات التالية في لوحة تحكم Railway:

```bash
SECRET_KEY=4m9f4d35!zqr-vc#h0$xxwt1ur19o^e##z0fsza7bqg4!ii^5)
DEBUG=False
ALLOWED_HOSTS=originrealestate-production.up.railway.app,*.railway.app
CSRF_TRUSTED_ORIGINS=https://originrealestate-production.up.railway.app,https://*.railway.app
SECURE_SSL_REDIRECT=False
```

### خطوات الإعداد في Railway:

1. **ربط المستودع:**
   - اذهب إلى Railway Dashboard
   - اختر "New Project"
   - اختر "Deploy from GitHub repo"
   - اختر المستودع: `ba3tezr/origin_real_estate`

2. **إضافة المتغيرات البيئية:**
   - اذهب إلى تبويب "Variables"
   - أضف المتغيرات المذكورة أعلاه

3. **إعدادات النشر:**
   - Railway سيكتشف تلقائيًا ملف `railway.json`
   - سيتم تثبيت المكتبات من `requirements.txt`
   - سيتم تشغيل التطبيق باستخدام Gunicorn

4. **قاعدة البيانات:**
   - ✅ يستخدم المشروع SQLite
   - ✅ ملف `db.sqlite3` موجود في المستودع
   - ✅ يحتوي على البيانات الحالية (المستخدمين، العقارات، إلخ)

---

## 🔐 بيانات الدخول الافتراضية:

```
اسم المستخدم: admin
كلمة المرور: admin123
```

---

## 🌐 رابط التطبيق:

**الرابط الحالي:** https://originrealestate-production.up.railway.app/

---

## 📝 ملاحظات مهمة:

1. **قاعدة البيانات SQLite:**
   - تم تضمين ملف `db.sqlite3` في المستودع
   - يحتوي على جميع البيانات الحالية
   - ⚠️ **تحذير:** أي تغييرات في البيانات ستُفقد عند إعادة النشر
   - للحفاظ على البيانات، يُنصح بعمل نسخة احتياطية دورية

2. **الملفات الثابتة (Static Files):**
   - يتم جمعها تلقائيًا عند النشر باستخدام `collectstatic`
   - يتم خدمتها باستخدام WhiteNoise

3. **الأمان:**
   - تم تعطيل `DEBUG` في الإنتاج
   - تم إضافة إعدادات الأمان المناسبة
   - المفتاح السري (SECRET_KEY) فريد ومعقد

4. **الأداء:**
   - يستخدم Gunicorn كخادم WSGI
   - WhiteNoise لخدمة الملفات الثابتة بكفاءة

---

## 🔄 إعادة النشر:

عند إجراء أي تغييرات:

```bash
git add .
git commit -m "وصف التغييرات"
git push origin main
```

Railway سيقوم تلقائيًا بإعادة النشر عند اكتشاف التغييرات في المستودع.

---

## 🆘 استكشاف الأخطاء:

### إذا واجهت مشكلة في التشغيل:

1. **تحقق من السجلات (Logs)** في Railway Dashboard
2. **تأكد من إضافة جميع المتغيرات البيئية**
3. **تحقق من أن الرابط في `ALLOWED_HOSTS` صحيح**

### الأوامر المفيدة للتشخيص:

في Railway Shell:
```bash
python manage.py check
python manage.py migrate
python manage.py collectstatic --noinput
```

---

## ✨ الميزات المتاحة:

- ✅ نظام إدارة العقارات
- ✅ إدارة العملاء والملاك
- ✅ العقود والمبيعات
- ✅ النظام المالي
- ✅ الصيانة
- ✅ التقارير
- ✅ API REST كامل
- ✅ واجهة Swagger للـ API

---

## 📞 الدعم:

للمساعدة أو الاستفسارات، يمكنك التواصل عبر GitHub Issues.

---

**تم التحضير بنجاح! 🎉**

